{
  "name": "lastfm-stats",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/process-data.js && astro build",
    "deploy": "npm run build && npx wrangler pages deploy dist/ --project-name lastfm-stats",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.0.6",
    "chart.js": "^4.5.1"
  }
}
